"""Tail — live stream of state change events (ndjson)."""

from __future__ import annotations

import typer


def run_tail() -> None:
    """Stream daemon events to stdout."""
    typer.echo("TODO: run_tail() — not yet implemented")
